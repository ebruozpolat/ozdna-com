{
  "name": "ozdna-app",
  "private": true,
  "type": "module",
  "description": "ozDNA content-provenance MVP monorepo (foundation). Lives under app/ in ozdna-com for now; to be extracted to its own repo. Canonical layout: plan/09-DEV-SETUP.md \u00a73.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.base.json && tsc --noEmit -p apps/api/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
